<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Webservices"
             x:Class="Webservices.MainPage">

    <StackLayout Orientation="Vertical">
        <Button x:Name="btn1" Text="Consultar Usuarios"></Button>
  

    <ListView x:Name="lstUsuarios">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal">
							<Label Text="Codigo:" TextColor="DarkMagenta"/>
							<Label Text="{Binding Codigo}"/>
							<Label Text="Nombre: " TextColor="DarkMagenta"/>
							<Label Text="{Binding Nombre}"/>
							<Label Text="Marca:" TextColor="DarkMagenta"/>
							<Label Text="{Binding Marca}"/>
							<Label Text="Contenido" TextColor="DarkMagenta"/>
							<Label Text="{Binding Contenido}"/>
							<Label Text="Valor" TextColor="DarkMagenta"/>
							<Label Text="{Binding Valor}"/>
							<Label Text="Descuento" TextColor="DarkMagenta"/>
							<Label Text="{Binding Descuento}"/>
                        <Image Source="{Binding Imagen}"/>

                        </StackLayout>                            
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    </StackLayout>

</ContentPage>
